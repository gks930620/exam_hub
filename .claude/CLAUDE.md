# 모든시험한번에보기 (exam-hub) — 웹 서비스 (React CSR)

> 🧭 **새 세션이면 [진행사항/00_지금_상태.md](../진행사항/00_지금_상태.md) 부터 읽으세요** — 현재 상태·실행법·다음 할 일·함정을 한 장에 정리.

Spring Boot 백엔드(REST API) + **React CSR 웹 프런트** 프로젝트입니다.
**실제 회사 조직처럼** 역할을 나눈 에이전트 팀이 기획 → 설계 → 구현 → 검증 → 배포 파이프라인으로 일합니다.

> ## 🚩 개발 방식 확정 (2026-07-29 — 사용자 지시, 변경 금지)
> - **웹 전용.** 모바일 앱(Flutter) 안 함 — `flutter_common/`은 **삭제됨**(2026-07-30).
> - **프런트는 무조건 CSR(React).** 예외 없음.
> - **기존 SSR(Thymeleaf pSEO)은 전부 CSR로 전환/제거한다.** 서버렌더 UI를 새로 만들지 않는다.
> - **SEO는 지금 신경 쓰지 않는다.** 필요해지면 나중에 Next.js(SSR) 등으로 별도 대응. 그 전까지 pSEO/sitemap은 보류.
> - 백엔드는 **REST API만** 담당(HTML 렌더 X), React가 그 API를 소비.
>
> ## 🚩 계정·커뮤니티 도입 (2026-08-06 — 사용자 결정, 기존 "기기식별" 방침을 대체)
> - **로그인은 소셜 단독** — 카카오·구글 OAuth2. 자체 이메일/비밀번호 계정은 만들지 않는다.
>   **단 매니저(운영자)는 예외**(2026-08-10 결정) — `/manager/login` 폼 로그인. 운영 계정이 카카오에
>   묶이면 그쪽이 막히는 날 운영이 멈추고, 인수인계가 개인 SNS 계정을 넘기는 꼴이 된다.
>   계정은 가입이 아니라 **로컬은 `data.sql`**(manager/pass1234), **운영은 DB 직접 INSERT** 로 만든다.
>   `MANAGER_USERNAME`/`MANAGER_PASSWORD` 환경변수 경로도 있지만 평소엔 비워 둔다(비밀번호 분실 시 복구용).
> - **기기식별(X-Device-Id)은 완전 제거** — `AppUser.deviceId` 폐기 → `Member`(provider+providerId). 기존 데이터 이관 없음.
> - **커뮤니티는 게시판 + 댓글**(자유·질문·후기). 추천·신고는 범위 밖.
> - **비로그인**은 시험 조회·커뮤니티 읽기까지. **내 시험·알림설정·캘린더·글쓰기는 로그인 필수**(401).
> - 설계 단일 기준: `설계/설계문서/08_계정과_커뮤니티.md`

## 제품 개요

- **무엇을 만드나**: 큐넷 등 각종 시험일정을 한곳에 모아 보여주고, 관심 시험의 D-day와 원서접수 시작/마감을 알려주는 **웹 서비스**.
- **⚠️ 범위: 웹 전용 + CSR(React).** 모바일 앱(Flutter) 안 함(`flutter_common/` **삭제됨**). 인터랙티브 기능(시험 검색·내 시험 등록·알림설정)이 전부 **React CSR 웹**에서 동작한다.
- **사용자는 누구인가**: 취준생·자격증 준비생·자격 수당이 필요한 직장인 — "접수 기간을 놓치면 다음 회차까지 반년 대기"가 페인 포인트.
- **알림 채널**: 웹 전용이므로 모바일 FCM 아님 → **웹 푸시 / 이메일 / (선택)카카오 알림톡**. (현재 `LogNotificationSender` 스텁 → 웹 채널로 전환 예정)
- **방향 전환(v1.1)**: 큐넷 국가기술자격 → **각종 시험 통합**(완료 — 시험 마스터 480종, 일정 없어도 노출). 유료화 제거 방향, 계정·커뮤니티 도입 확정. `설계/시험데이터/02_전수목록.md`·`08_계정과_커뮤니티.md` 참고.

## 스택 / 저장소 구조

| 영역 | 스택 | 경로 | 테스트 명령 |
|---|---|---|---|
| 백엔드 (REST API) | Spring Boot 3.3 (Gradle, Java 17, JPA) | 저장소 루트 (`src/`) — 패키지 `com.test.test`, 도메인 `com.test.test.exam.*` | `./gradlew test` |
| 웹 프런트 | **React + Vite + TypeScript (CSR)** | `frontend/` | `npm test` / `npm run build` |
| 배포 | Railway | 절차: `.claude/skills/railway-deploy/SKILL.md` | — |
| ~~앱~~ | ~~Flutter~~ | **삭제됨(2026-07-30)** — 웹 전용, 앱 미개발 | — |
| ~~SSR 웹~~ | ~~Thymeleaf pSEO~~ | **제거됨 — CSR 전환(2026-07-29). SEO는 추후 Next.js** | — |

이 프로젝트만의 특이사항:

- **인증**: 소셜 로그인(카카오·구글 OAuth2) → `Member` + JWT(`Authorization: Bearer`). 매니저(운영자)만 `/manager/login` 폼 로그인. 구 `X-Device-Id` 기기식별은 **폐기·전환 완료**. 설계는 `08_계정과_커뮤니티.md`.
- **DB**: 로컬 = **파일 H2**(`.localdb/`, MODE=MySQL, ddl-auto=update) / 운영 = Railway MySQL (env 미설정 시 fail-fast).
  - ⚠️ 컨벤션 §5-2 의 "로컬은 매 기동 초기화" 에 대한 **명시적 예외**(2026-09-01 사용자 결정).
    시험일정 2,452건을 매 기동 다시 넣느라 5~9분씩 걸렸다. 파일로 두니 **재기동 27초**.
    아직 사용자 데이터가 쌓이지 않는 단계라 잃을 게 없어서 지금 바꿨다.
  - **초기화하려면 `.localdb/` 폴더를 지우고 다시 켠다**(= 예전 create 와 같은 상태, 첫 기동 9분).
    스키마를 크게 바꿔 `update` 가 못 따라갈 때도 이렇게 한다.
  - URL 에 `AUTO_SERVER=TRUE` 가 붙어 있다 — 없으면 devtools 재기동 때 파일 잠금이 겹쳐 기동이 깨진다.
- **외부 연동 상태**: `QnetApiScheduleSource`(큐넷 실 API)는 **구현됨**(RestClient, 컴파일 검증 — 실 네트워크 미검증). 발송은 `LogNotificationSender`(로그 스텁) — 웹 전용이라 실발송은 **웹 푸시/이메일**로 전환 예정(모바일 FCM 아님). 로컬은 `qnet.api.enabled=false`로 Mock+시드 수집 + 로그 발송으로 전 구간 동작.
- **각종 시험 통합**: 비큐넷 시험은 `resources/seed/non_qnet_exams.json` + `SeedFileScheduleSource`로 로컬 DB 적재(16종). `Certificate.category`로 분류.
- **시드**: 시험 데이터는 자바 로더가 적재한다. `CertificateMasterInitializer`(마스터 480종 — **운영에서도 돈다**),
  `ExamDataInitializer`·`SeedFileScheduleSource`(일정 있는 데모 — `@Profile("!prod")`).
  `data.sql` 은 **매니저 계정만** 넣는다(H2 일 때만 실행 → 운영 제외).
- 시각은 전부 KST — `TimeUtil`(Asia/Seoul) 경유.

## 팀 구조 (회사 조직)

| 에이전트 | 역할 | 수정 권한 |
|---|---|---|
| `product-planner` | 기획자 — 사용자 관점 동작 흐름·인수 조건 정의, 기획 문서 관리 | 문서만 |
| `designer` | 디자이너 — 화면 정의서(상태별 UI 포함)·디자인 토큰 관리 | 문서·토큰만 |
| `senior-dev` | 선임개발자(테크 리드) — ERD·API 명세서 작성, TDD 테스트 선작성, 개발 중 코드리뷰, 기술 결정 | O |
| `backend-dev` | 백엔드개발자 — Spring Boot **REST API** 구현 (HTML 렌더 안 함) | O (백엔드만) |
| `frontend-dev` | 프론트개발자 — **React CSR 웹 구현(활성)**. `frontend/` (Vite+TS). API 소비 | O (웹만) |
| `app-dev` | ~~앱개발자(Flutter)~~ — **비활성. 앱 삭제됨, 웹 전용이라 사용 안 함** | — |
| `qa` | QA — 기획서로 테스트 케이스 설계, 실제 제품을 시나리오로 시험(경계·비정상·탐색·회귀) | X |
| `devops` | CI/CD — git·GitHub Actions·Railway 배포 | O (설정·워크플로만) |
| `builder` *(공통)* | 스택에 안 걸리는 잡일(스크립트·설정·문서). 개발자 영역 밖의 폴백 | O |

> 공통 planner/reviewer/researcher는 뺐다 — 기획은 product-planner·senior-dev가, 코드 검토는
> senior-dev·qa가 이미 맡아서 겹치기 때문. 필요해지면 최상위 `.claude/agents/`에서 파일만 가져오면 된다.

## 일하는 흐름 (파이프라인)

```
product-planner(사용자 흐름·인수 조건) → designer(화면 정의서)
→ senior-dev(기획서에서 API 명세서 도출 + ERD + 명세 기반 테스트 선작성 = Red)
→ backend-dev(REST API) ∥ frontend-dev(React CSR)   (테스트를 통과시키며 병렬 구현 = Green)
→ senior-dev(개발 중 코드리뷰 — 컨벤션·클린코드·스택 간 정합성, 수정은 개발자가)
→ qa(테스트 케이스 설계·시나리오 시험) — 결함은 해당 개발자로 되돌림
→ devops(커밋·배포)
```

## 작업 방식 (TDD — 이 프로젝트의 핵심 규칙)

**항상 TDD다. 프로덕션 코드보다 테스트 코드가 먼저다. 예외 없음.**

1. **테스트가 명세다** — senior-dev가 API 계약·화면 요구를 테스트 코드로 먼저 작성한다(실패 상태 = Red). 테스트 없이 개발자에게 일을 넘기지 않는다.
2. **개발자는 테스트를 통과시킨다(Green)** — senior-dev의 테스트 수정 금지. 테스트가 틀렸다고 판단되면 senior-dev에게 되돌린다.
3. **테스트에 없는 코드가 필요해지면** — 계약·공개 동작은 senior-dev에게 테스트 추가를 요청하고, 내부 구현 세부는 개발자가 직접 테스트를 먼저 작성(실패 확인)한 뒤 구현한다. 어떤 경우에도 순서는 테스트 → 코드다.
4. **계약 밖 변경 금지** — API 응답 필드를 임의로 추가/변경하면 다른 스택이 깨진다. 계약 변경은 senior-dev를 통해서만.
5. **qa 통과가 완료 조건** — qa는 개발자 테스트를 재실행하는 게 아니라, 기획서로 테스트 케이스를 설계해 실제 제품을 시험하는 독립 관문이다. 테스트 없이 들어온 프로덕션 코드는 qa가 결함으로 리포트한다.
## 문서 위치

문서는 **성격으로 나뉜다. 폴더를 보면 그 문서가 언제까지 살아 있을지 알 수 있다.**

| 폴더 | 성격 | 수명 |
|---|---|---|
| `진행사항/` | 지금 뭘 하고 있나 | **끝나면 지운다** |
| **`설계/시험데이터/`** | ⭐ **시험 데이터를 어떻게 끌고 오나** — 이 서비스의 심장 | 영구 |
| `설계/설계문서/` | 왜 이렇게 만들었나 | 영구 |
| `설계/운영/` | 서비스를 굴리며 계속 보는 것 | 영구 |
| `설계/디자인_Halo/` | 디자인 원본 | 영구 |

> 새 문서를 만들기 전에 **"이건 끝나면 지울 것인가?"** 를 먼저 묻는다.
> 지울 것이면 `진행사항/`, 계속 볼 것이면 `설계/` 아래다.

### ⭐ 설계/시험데이터 — 기능보다 이게 먼저다

시험 480종 중 **418종에 아직 일정이 없다.** 기능이 아무리 많아도 "언제 접수하는지"가
없으면 쓸 이유가 없는 서비스다. 데이터 관련 문서는 흩어놓지 않고 전부 이 폴더에 모았다.

| 문서 | 언제 여나 |
|---|---|
| **`00_한장요약`** | **여기부터.** 현황 숫자·네 갈래 수집 방식·큐넷 전략·다음 순서 |
| `01_수집방법_카탈로그` | 새 소스를 붙일 때 — 시험별로 어디서 어떻게 가져오나 |
| `02_전수목록` | 담아야 할 시험 약 730종 인벤토리 + 적재 현황 |
| `03_스크래퍼_정비` | **스크래퍼가 깨졌을 때** — 사이트별 실측 기록 |
| `04_마스터_실사` | **"어떤 시험이 빠졌나"** — 큐넷 613종 대조(2026-08-10) |
| `큐넷_API/` | 큐넷 API 를 만질 때 — 7개 분석 + 실제 응답 샘플 |

> 화면에서도 같은 내용을 본다 — 매니저 `/admin` 의 **데이터 지도**(`DataSourceCatalog.java`).
> **수집 소스를 추가하면 그 코드도 같이 고친다.** 안 그러면 매니저에게 "수기로 넣으세요"라고
> 안내한 채라 사람이 헛일을 한다.

### 진행사항 (3개만 — 늘어나면 성격을 다시 본다)

| 문서 | 무엇 |
|---|---|
| `00_지금_상태.md` | **새 세션은 여기부터.** 현재 상태·실행법·함정 |
| `01_AI_작업큐.md` | AI 가 할 코드 작업 |
| `02_내가_할일.md` | 사람만 할 수 있는 것(키 발급·결정) |

> 완료 기록은 문서로 남기지 않는다 — **git log 가 대신한다.**

### 설계/설계문서 — 왜 이렇게 만들었나

| 문서 | 무엇 | 신뢰도 |
|---|---|---|
| `01_제품기획` · `02_요구사항` | 제품 정의, FR/NFR 번호(코드 주석이 참조) | 🟡 v1.0 원본 — 상단 현행화 노트만 |
| `04_데이터모델_ERD` · `05_API명세` · `06_아키텍처` | 스키마·계약·구조 | 🟡 상동 |
| **`08_계정과_커뮤니티`** | 로그인·매니저 권한·커뮤니티 ERD·API 계약 | 🟢 최신·구현 완료 |

### 설계/운영 — 서비스를 굴리며 보는 것

| 문서 | 언제 여나 |
|---|---|
| `01_카카오_설정` | 로그인·알림톡 키를 잡을 때, 배포 도메인이 늘 때. **KOE205 도 여기** |
| `02_외부키_발급` | 큐넷·구글·메일 키가 필요할 때 |
| `03_일정_직접입력` | **매니저 매뉴얼** — 운영자가 시험 일정을 손으로 넣는 법 |

- **디자인: Halo** — 원칙은 `설계/디자인_Halo/halo-design-kit/HALO-디자인-가이드.md`.
  **단일 출처는 `frontend/src/styles/halo-tokens.css` 의 `:root` 토큰** — 새 컴포넌트는 토큰만 조합하고 색을 하드코딩하지 않는다.
  앱 전용 컴포넌트는 `frontend/src/styles/app.css`. 다크 모드는 `<html data-theme>` 방식(`frontend/src/theme.ts`)

## 이 프로젝트의 규칙

- **커밋·푸시·배포는 사용자가 명시적으로 요청할 때만** — 실행은 devops 담당.
- **배포 전 조건**: 테스트 통과 + qa 판정 통과. 절차는 railway-deploy 스킬.
- **새 라이브러리 도입은 senior-dev 결정 사항** — 개발자가 임의로 추가하지 않는다.
- **코드 컨벤션**: `code-convention` 스킬이 기준이다 (DTO class 규칙, 계층 구조, 백엔드 테스트는 컨트롤러 통합테스트만 등). 문서와 코드가 어긋나면 문서에 맞춰 코드를 고친다. ※ 알려진 미준수: DTO `record` 잔존(`CertificateDtos`·`FavoriteDtos`·`MeDtos` 등) — `진행사항/01_AI_작업큐.md` §4 참고.
- **금지**: 요청받지 않은 대규모 리팩터링, 자기 영역 밖 스택의 코드 수정.
