# 다음 할 일 (작업 큐)

> 갱신 2026-09-03. **AI 가 할 코드 작업**만. 사람이 할 것은 [02_내가_할일.md](02_내가_할일.md),
> 현재 상태는 [00_지금_상태.md](00_지금_상태.md).
>
> 완료 기록은 여기 안 쌓는다 — **git log** 가 대신한다.

---

## ✅ 큐넷 밖 국가기술자격 — 완료(상의·KCA·Kdata)

컴퓨터활용능력·정보보안기사·빅데이터분석기사 등은 시행처가 큐넷이 아니다
([04_마스터_실사 §3](../설계/시험데이터/04_마스터_실사.md)). **컴활은 취준 수요가 가장 큰 축**이라 급하다.

- [x] 대한상공회의소 — **완료**(유통관리사·전자상거래·한글속기 9종 19건). ⚠️ **컴활은 상시시험이라 대상 아님**
- [x] KCA(cq.or.kr) — **완료**(정보보안기사·정보통신기사·무선설비·전파전자통신 18건)
- [x] dataq.or.kr — **완료**(7종 22건: 빅데이터분석기사·ADP·ADsP·SQLP·SQLD·DAP·DAsP)
- [ ] 시행처를 각 사이트에서 **확인**하고 마스터의 `agency` 를 고칠 것 — 지금은 통념으로 적혀 있다

## 🟡 2. "일정 없음 190종" 크롤링으로 줄이기 (사용자 지시 2026-09-01)

> 방침: **도저히 안 되는 것만 수기.** 나머지는 전부 크롤링을 시도한다.
> 기존 스크래퍼(KCA·상의·Kdata·어학) 패턴 그대로 — 픽스처 저장 → 파서 테스트 → 소스 등록 →
> `DataSourceCatalog` 를 같이 고친다(안 고치면 매니저가 헛일한다).

우선순위 = 종수 × 페이지 구조 단순함:

- [x] ~~국시원~~ **완료(2026-09-01)** — 16종 수집(KuksiwonScheduleSource). 요양보호사는 상시라 제외. 일정없음 190→174
- [ ] **생산성본부 9종** — ⚠️ 조사 결과(2026-09-01) 본문이 전부 JS 렌더링이라 순수 HTTP 로는 날짜가 안 온다(G-TELP 부류). 헤드리스 브라우저를 쓸지, 수기로 둘지 결정 필요
- [ ] **금융 3처 13종** — ⚠️ 금투협도 JS 셸(1.7KB)만 온다. 내부 XHR 경로 조사 필요
- [x] ~~TOPIK~~ **완료(2026-09-01)** — 지필 6회차 × I·II(TopikScheduleSource). IBT·말하기는 마스터에 없어 제외
- [ ] **FLEX 7종** — ⚠️ 일정 페이지(44KB)에 날짜가 없다(JS). 내부 경로 조사 필요
- [ ] **스포츠지도사 4종** — ⚠️ 루트가 400. 진입 경로부터 조사 필요
- [ ] **한자 (어문회·대한검정회)** / **HSK** (기존 작업큐 항목과 합침)
- [ ] 큐넷 전문자격 9종(경비지도사 등) — 기술자격과 다른 API/페이지인지 조사부터

**크롤링 불가 확정(수기 유지)**: 공무원 9종(공고 PDF — 연초 1회 수기가 더 싸다).
**대상 아님 확정(넣지 않음)**: IT 벤더(AWS·Cisco·MOS…)·운전면허·TS 자격 — 상시 예약제라 마감이 없다.
**데이터 자체가 없음**: 큐넷 4자리 중 ~40종 — API 에 2026 일정이 없다(미시행·격년). 뜨면 자동으로 들어온다.

## 🟡 3. 수집 소스 넓히기 — 순서는 [06_시행처_전수조사](../설계/시험데이터/06_시행처_전수조사.md) §5

- [x] ~~한국생산성본부 12종~~ — `KpcScheduleSource` 구현·검증 완료(2026-09-08). ITQ 5·GTQ 3 즉시 유입,
      ERP 4 는 접수가 열리면 들어온다. **"수기 필수"였던 곳이 실은 자동으로 됐다.**
- [x] ~~YBM 7종~~ — `YbmScheduleSource` 구현·검증 완료(2026-09-08). 페이지 6곳 → **일정 111건 즉시 유입**,
      옛 `ToeicScheduleSource` 는 대체·삭제. **BCT 1종만 남았다** — 시행처 사이트가 열리지 않아 수기.
- [x] ~~금융투자협회 9 + 금융연수원 8~~ — `KofiaScheduleSource`·`KbiScheduleSource` 구현·검증 완료(2026-09-08).
      **일정 페이지가 표를 서버렌더로 안 그린다** — 화면이 부르는 조회 주소를 그대로 쓴다(로그인·키 없음).
- [x] ~~FLEX 7 · 공인회계사회 4 · 어문회 4 · 공인회계사 1~~ — 구현·검증 완료(2026-09-08).
- [x] ~~금감원 보험 3~~ — **목록에서 뺐다**(2026-09-08 사용자 결정). 시험 사이트가 없어 일정을 구할 길이 없다.
      폐지가 아니라 `EXCLUDED`(다루지 않음)로 기록 — `seed/certificate_lifecycle.json`, 매니저 변천사 화면.
- [x] ~~**체육공단 4**~~ — `KspoScheduleSource` 구현·검증 완료(2026-09-08). **급수별 페이지를 4번 따로 읽는다**
      (`schedPlan.kspo?QF_GRADE_CD=`). 시험일은 같은데 접수가 급수마다 달라, 하나를 넷에 붙였으면 셋이 틀렸다.
- [x] ~~세무사회 4 · 대한검정회 2 · TESAT 1 · DIAT 1~~ — 구현·검증 완료(2026-09-08)
- [x] ~~보험연수원 2 · 매경TEST 1 · HSK 1~~ — 구현·검증 완료(2026-09-08)
- [ ] TEPS S&W 2 · G-TELP 2(봇 차단) · 소방청 2 · 한국어교원 2 · 국사편찬위 2 · 삼일 3 · CPPG · 신용관리사
- [ ] **(사람)** JLPT·HSK 는 시행처가 우리 수집기를 UA 로 거부해 수기로 돌렸다(2026-09-09).
      브라우저인 척하면 읽히지만 우회는 안 했다 — 그렇게 할지는 사람이 정할 일이다.
- [x] ~~TEPS 연간일정~~ — **못 붙인다.** `/receiption/examschedule` 은 JS 로 그려서 순수 HTTP 에
      표가 없다(2026-09-09 확인). 첫 화면이 유일한 서버렌더 원본이고, 거기엔 접수 중인 회차만 실린다.
      **1건이 정상**이다 — 시행처가 그 이상 공개하지 않는다.
- [ ] **ICQA 4종은 못 붙인다** — 정기시험 일정을 구글 캘린더 링크로만 안내한다(공지 610). 수기로 남긴다
- [ ] **(사람)** 상공회의소한자 1·2급 — 시행처 종목 메뉴에서 사라졌다. 폐지인지 확인해 변천사를 확정해 주세요
- [ ] 인터넷정보관리사 1 · DELF·DALF 2 — 일정 원본을 아직 못 찾았다

일정 없음 151종을 2026-09-02 에 시행처별로 다시 두드려 분류했다. 순수 HTTP 에 날짜가 보이는 곳(C)부터.

- [ ] **기존 스크래퍼 확장(가장 싸다)**: 상의 무역영어 3·상공회의소한자 2 / KCA 디지털포렌식 2급 / TEPS Speaking·Writing
- [x] ~~SQLD 중복 시험 합치기~~ — 2026-09-02 변천사(개칭)로 정리했다(`seed/certificate_lifecycle.json`). 옛 이름은 검색에서 빠진다. 코드를 마스터 쪽으로 맞추고 한쪽을 변천사(개칭)로
- [ ] **큐넷 전문자격 게시판** — 경비지도사·관광통역안내사 등 A 그룹 전문자격 ~10종. 기술자격 API 밖
- [ ] 새 스크래퍼(날짜 확인됨): 한국어문회 한자 · 금감원 CPA · ICQA 3 · KAIT 2 · CPPG · 신용관리사 · 국회직 2 · 보험연수원 2 · 삼일 2
- [ ] 확인 필요: 리눅스마스터(표 형식) · 국립국어원 한국어교원(PDF?)
- [ ] **D 그룹(JS 렌더링 ~60종)** — 헤드리스 브라우저 도입 여부 결정 후 한꺼번에. 그전까지는 수기


## 🟡 4. 배포 (사람 결정 대기)

- [ ] **(사람)** GitHub 원격 저장소를 만들지 결정
- [ ] Railway 배포 — 환경변수 목록은 [02_내가_할일 §4](02_내가_할일.md)
- [ ] 배포 후 확인: `/healthz` · `/`(화면) · `/api/certificates/browse` · 소셜 Redirect URI 에 도메인 추가

## 🟢 5. 다듬기

- [ ] **어학시험에 "필기"가 붙는다** — `ExamType` 이 WRITTEN/PRACTICAL 둘뿐이라 토익스피킹·TSC 같은
      말하기 시험도 "필기 접수 마감"으로 나간다. 사실과 다르다. 어학 카테고리 전반의 문제라
      (a) 구분값을 하나 더 두거나 (b) 한 종류뿐인 시험은 구분을 안 보여 주는 쪽으로 정해야 한다.
      회차 쪽은 2026-09-08 에 정리했다(`ExamSchedule.roundLabel()` — 지어낸 회차는 안 보인다).
- [ ] **시드 코드 통일** — 같은 시험에 `non_qnet_exams.json` 과 `exam_master.json` 이 서로 다른 코드를 붙인다(51종).
      비큐넷 시드는 `@Profile("!prod")` 라 **로컬은 시드 코드, 운영은 마스터 코드**가 붙는다.
      수집은 이름으로도 찾아 붙지만, `coveredExamCodes()` 는 코드로만 맞춰 봐서 소스마다 두 코드를 다 밝혀야 한다
      (`SeedCodeAlias`). 시드 코드를 마스터 코드로 통일하면 그 클래스와 별칭이 통째로 사라진다.
- [ ] DTO `record` → class (컨벤션 §0 미준수 잔존: `CertificateDtos`·`FavoriteDtos`·`MeDtos` 등)
- [ ] 커뮤니티: 검색, 내가 쓴 글 모아보기
- [ ] 알림 발송 실패 재시도 정책
- [ ] 상세 화면 slug 라우트(`/cert/by-slug/...`)
- [ ] 로딩 스켈레톤·토스트 등 UX
- [ ] **수집 심장박동 화면** — 실행 기록 테이블(`CrawlLog`)과 저장 코드는 **이미 있다**(소스마다 성공·실패·건수). 없는 건 그걸 읽어 "큐넷 3일째 실패"를 띄우는 화면·알림이다. 지금 고장 신호는 매니저 할 일의
      "회차 끊김 확인"(자동 소스 1년 무소식)뿐이라 스크래퍼가 조용히 죽으면 1년 뒤에야 안다. 시험 단위가 아니라
      **소스 단위** 경고("큐넷 3일째 실패")가 맞다 — 시험 단위로 60일 기준을 걸어 봤더니 114종이 헛경고였다(2026-09-02).
